<FillUp
  class="github-card"

  @breakpoints={{hash
    vertical=(fill-up-between 0 600)
    horizontal=(fill-up-gte 600)
  }}
as |F|>
  {{#if F.breakpoints.vertical}}
    <GithubCard::Vertical @repo={{this.repo}} />
  {{else}}
    <GithubCard::Horizontal @repo={{this.repo}}/>
  {{/if}}
</FillUp>