<FillUp
  class="github-card"

  @breakpoints={{hash
    vertical=(fill-up-between 0 450)
    horizontal=(fill-up-gte 450)
  }}
as |F|>
  {{#if F.breakpoints.vertical}}
    <GithubCard::Vertical @repo={{this.repo}} />
  {{else}}
    <GithubCard::Horizontal @repo={{this.repo}}/>
  {{/if}}
</FillUp>